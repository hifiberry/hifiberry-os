Source: hifiberry-aes67
Section: sound
Priority: optional
Maintainer: HiFiBerry <support@hifiberry.com>
Build-Depends: debhelper-compat (= 13),
               dh-sequence-python3,
               python3-all,
               python3-setuptools,
               python3-pytest,
Standards-Version: 4.1.4
Homepage: https://www.hifiberry.com/

Package: hifiberry-aes67
# Extension metadata, read by the WebUI's extension catalog. See
# configurator/extensions/catalog.py: every card is built from the apt record,
# and any field omitted here silently falls back to a default (the raw package
# name, category "tool"). dpkg strips the XB- prefix in the built record.
XB-Hifiberry-Extension: yes
XB-Extension-Name: AES67
XB-Extension-Category: player
XB-Extension-Needs-Reboot: no
# Served by hifiberry-webui itself, so it resolves before the extension is
# installed and on a device with no internet access.
XB-Extension-Icon-Url: /images/svg/network.svg
Architecture: all
Depends: ${python3:Depends}, ${misc:Depends}, systemd, pipewire (>= 1.1),
         hifiberry-configurator (>= 2.17.6)
Description: HiFiBerry AES67 receiver
 Receives AES67 audio from a Dante network and plays it through the HiFiBerry
 DAC using PipeWire. Streams announced over SAP are discovered automatically and
 exposed over a REST API for selection from the web interface.
 .
 This is a listening endpoint rather than a clock-synchronised AES67 device: it
 does not run a PTP daemon, so it does not share the Dante clock.
