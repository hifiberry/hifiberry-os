#!/bin/bash
set -e

case "$1" in
    remove|upgrade|deconfigure)
    # No system-level service management for user units
        ;;
    
    failed-upgrade)
        ;;
    
    *)
        echo "prerm called with unknown argument \`$1'" >&2
        exit 1
        ;;
esac

#DEBHELPER#

exit 0
