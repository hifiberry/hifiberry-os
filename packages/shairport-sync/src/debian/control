Source: hifiberry-shairport
Section: sound
Priority: optional
Maintainer: HiFiBerry <support@hifiberry.com>
Build-Depends: debhelper-compat (= 13),
               libssl-dev,
               libavahi-client-dev,
               libpopt-dev,
               libconfig-dev,
               libdaemon-dev,
               libsoxr-dev,
               avahi-daemon,
               libplist-dev,
               libplist-utils,
               libsodium-dev,
               libgcrypt-dev,
               libavutil-dev,
               libavcodec-dev,
               libavformat-dev,
               uuid-dev,
               libsystemd-dev,
               libglib2.0-dev,
               libasound2-dev,
               vim-common,
               xxd,
               autoconf,
               automake,
               libtool,
               pkgconf,
               git,
               ca-certificates,
               wget
Standards-Version: 4.6.0
Homepage: https://github.com/mikebrady/shairport-sync
Vcs-Git: https://github.com/mikebrady/shairport-sync.git
Vcs-Browser: https://github.com/mikebrady/shairport-sync

Package: hifiberry-shairport
# Extension metadata, read by the WebUI's extension catalog. See
# configurator/extensions/catalog.py: every card is built from the apt record,
# and any field omitted here silently falls back to a default (the raw package
# name, category "tool"). dpkg strips the XB- prefix in the built record.
XB-Hifiberry-Extension: yes
XB-Extension-Name: AirPlay
XB-Extension-Category: player
XB-Extension-Needs-Reboot: no
# Served by hifiberry-webui itself, so it resolves before the extension is
# installed and on a device with no internet access. Must be genuine stroke
# geometry at viewBox -1 -1 23 23, like the rest of the set.
XB-Extension-Icon-Url: /images/svg/airplay.svg
Architecture: any
Depends: ${shlibs:Depends}, ${misc:Depends},
         libssl3 | libssl3t64,
         libavahi-client3,
         libpopt0,
         libconfig11 | libconfig9,
         libdaemon0,
         libsoxr0,
         avahi-daemon,
         libplist-2.0-4 | libplist3,
         libsodium23,
         libgcrypt20,
         uuid-dev,
         libsystemd0,
         libcap2-bin,
         libglib2.0-0 | libglib2.0-0t64,
         adduser,
         hifiberry-configurator
Provides: shairport-sync, nqptp
Conflicts: shairport-sync, nqptp
Replaces: shairport-sync, nqptp
Description: AirPlay audio player with NQPTP (HiFiBerry build)
 A multi-room synchronized audio player for AirPlay devices,
 customized for HiFiBerry devices. Includes NQPTP daemon for
 network clock synchronization required by AirPlay 2.
 .
 This package includes:
  - Shairport Sync AirPlay receiver
  - NQPTP network time synchronization daemon
  - Metadata reader for AirPlay information
  - Custom HiFiBerry integration scripts
