// IMPORTANT: This is a default configuration template. 
// During package installation, this file is copied to /etc/shairport-sync.conf
// if that file doesn't already exist.
// At startup, the system will create a runtime copy at 
//      /var/lib/shairport/shairport-sync.conf
// with system-specific settings applied.
// Edit /etc/shairport-sync.conf to make permanent changes.

general = {
  name = "HiFiBerry"; // This is the name the device will advertise to iTunes
  version = "1"; // AirPlay version: "1" for legacy, "2" for AirPlay 2 (default if not specified)
  interpolation = "basic";
  output_backend = "alsa";
  mdns_backend = "avahi";
  volume_control_profile = "standard";
  drift_tolerance_in_seconds = 0.015;
  ignore_volume_control = "no";
  volume_range_db = 60;
  regtype = "_raop._tcp";
  playback_mode = "stereo";
};

sessioncontrol = {
  allow_session_interruption = "yes";
  session_timeout = 120;
};

alsa = {
  mixer_control_name = "Softvol";
  mixer_device = "hw:0";
  output_format = "S16_LE";
  output_rate = 44100;
};

metadata = {
  enabled = "yes";
  include_cover_art = "yes";
  pipe_name = "/tmp/shairport-sync-metadata";
  pipe_timeout = 5000;
  socket_address = "127.0.0.1";
  socket_port = 5555;
  socket_msglength = 65000;
};

// These are parameters for the "AirPlay 2" backend
airplay_2 = {
  use_negotiated_ports = "yes";
};

diagnostics = {
  log_verbosity = 0;
};