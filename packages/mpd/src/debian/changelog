hifiberry-mpd (0.24.13.5) stable; urgency=medium

  * Announce the pretty hostname over Zeroconf, like the other players.
    start-shairport.sh and start-librespot.sh both read `hostnamectl hostname
    --pretty` (falling back to the hostname, then "HiFiBerry") and announce
    that, but MPD kept the static zeroconf_name "HiFiBerry MPD" from the
    shipped config -- so on one device the same speaker showed up under two
    different names, and on a multi-device setup every MPD looked identical.
    configure-mpd now writes zeroconf_name on each start, using the same
    fallback order. Names containing " or \ are escaped for MPD's config
    tokenizer, and the name is truncated to 63 bytes on a character boundary
    to stay within Avahi's service-name limit.
  * Keep MPD's "%h" placeholder out of the announced name. MPD expands "%h" in
    zeroconf_name to the hostname after parsing the config, so a pretty
    hostname containing the token was announced as something other than what
    was configured, and could grow back past the 63-byte limit. There is no
    escape for it, so the "%" is dropped.
  * NOTE: zeroconf_name in ~/etc/mpd.conf is now managed by configure-mpd and
    is overwritten on every MPD start. It previously passed through verbatim
    and survived restarts; a hand-set name there will now be replaced by the
    pretty hostname. Set the device name via the hostname API (or
    `hostnamectl set-hostname --pretty`) instead, which renames MPD, AirPlay
    and Spotify Connect together.

 -- HiFiBerry <support@hifiberry.com>  Fri, 07 Aug 2026 10:00:00 +0200

hifiberry-mpd (0.24.13.4) stable; urgency=medium

  * Stop shipping the upstream user mpd.socket. Its postinst enabled it, so
    MPD started socket-activated; on that path listen_global_init() returns
    before setting listen_port, ZeroconfInit() logs "No global port, disabling
    zeroconf" and _mpd._tcp is never advertised despite zeroconf_enabled "yes".
  * Clean up the enable state that socket left behind on upgrade: the new
    maintainer scripts no longer mention mpd.socket, so the .wants symlink and
    the deb-systemd-helper state file would survive as dangling leftovers, and
    a socket still loaded in the user manager would keep holding port 6600.
  * Drop debian/hifiberry-mpd.service. Its entire content was the string
    "mpd.service", so debhelper shipped that as the body of
    /usr/lib/systemd/system/hifiberry-mpd.service: an unparsable unit that
    every postinst then tried to restart. Nothing referenced it.

 -- HiFiBerry <support@hifiberry.com>  Thu, 06 Aug 2026 16:30:00 +0200

hifiberry-mpd (0.24.13.3) stable; urgency=medium

  * Add Breaks: hifiberry-audiocontrol (<< 0.8.8). Older audiocontrol still
    carries this player's inline entry in audiocontrol.json; combined with
    the new /etc/audiocontrol/players.d/mpd.json conffile that duplicates
    into two MPD controllers, since audiocontrol merges includes without
    deduplicating against the legacy players array.

 -- HiFiBerry <support@hifiberry.com>  Mon, 03 Aug 2026 16:00:00 +0200

hifiberry-mpd (0.24.13.2) stable; urgency=medium

  * Ship the ACR player definition as /etc/audiocontrol/players.d/mpd.json.
    The definition used to live in audiocontrol.json, which meant the package
    that provides mpd and the file that defines the player were owned by
    different packages.

 -- HiFiBerry <support@hifiberry.com>  Mon, 03 Aug 2026 14:13:05 +0200

hifiberry-mpd (0.24.13.1) stable; urgency=high

  * Updated to MPD v0.24.13 from upstream
  * Security: v0.24.11 fixes a path traversal that let clients escape the
    music directory via "listfiles ..". This was reachable without a password
    on our default configuration, which binds to 0.0.0.0 and grants
    default_permissions "read,add,control" (listfiles only needs read).
  * Security: v0.24.11 fixes a stack buffer overflow in the pcm decoder and
    disallows newlines and invalid UTF-8 in song URIs
  * v0.24.13 fixes a pipewire file descriptor leak after connect failure
  * v0.24.12 fixes a v0.24.11 regression with empty URIs in "lsinfo"/"add"

 -- HiFiBerry <support@hifiberry.com>  Wed, 15 Jul 2026 12:00:00 +0000

hifiberry-mpd (0.24.8.4) stable; urgency=low

  * Enable NFS support (libnfs) and the proxy database plugin (libmpdclient)
  * Request both meson features explicitly so a missing build dependency fails
    the build instead of silently dropping the feature
  * Thanks to Jose P. Ferrero for the report and patch

 -- HiFiBerry <support@hifiberry.com>  Wed, 15 Jul 2026 09:00:00 +0000

hifiberry-mpd (0.24.8.3) stable; urgency=low

  * Fix configure-mpd: re-copy default config when user config is empty (0 bytes)

 -- HiFiBerry <support@hifiberry.com>  Thu, 27 Feb 2026 09:00:00 +0000

hifiberry-mpd (0.24.8.2) stable; urgency=low

  * Add mpd-reconcile helper script to run configure-mpd as the HiFiBerry user
    and trigger MPD library update
  * Add hifiberry-mpd-reconcile.service oneshot unit for post-mount MPD
    reconciliation workflows

 -- HiFiBerry <support@hifiberry.com>  Thu, 26 Feb 2026 19:00:00 +0000

hifiberry-mpd (0.24.8.1) stable; urgency=low

  * Updated to MPD v0.24.8 from upstream
  * Latest upstream release with bug fixes and improvements
  * Includes fixes from v0.24.6, v0.24.7, and v0.24.8

 -- HiFiBerry <support@hifiberry.com>  Sat, 01 Feb 2026 12:00:00 +0000

hifiberry-mpd (0.24.5.7) stable; urgency=low

  * Fixed unstripped-binary lintian errors by using debugoptimized build
  * Added explicit strip commands for mpd and mpc binaries
  * Ensured debug symbols are available for proper stripping

 -- HiFiBerry <support@hifiberry.com>  Thu, 09 Oct 2025 21:30:00 +0000

hifiberry-mpd (0.24.5.6) stable; urgency=low

  * Fixed lintian unstripped-binary errors by ensuring proper stripping
  * Removed embedded documentation to fix JavaScript library warnings
  * Fixed maintainer script systemctl calls to use deb-systemd-helper
  * Fixed changelog line length warnings

 -- HiFiBerry <support@hifiberry.com>  Thu, 09 Oct 2025 21:00:00 +0000

hifiberry-mpd (0.24.5.5) stable; urgency=low

  * Created separate start-mpd script for user validation and service startup
  * Added user validation check - only allow running as root or user in
    /etc/hifiberry.user
  * Updated systemd service to use start-mpd wrapper script instead of
    direct MPD execution
  * Added manual page for start-mpd script
  * Exit with code 0 to prevent systemd restart attempts on authorization
    failure
  * Improved service architecture consistency with other HiFiBerry services

 -- HiFiBerry <support@hifiberry.com>  Thu, 09 Oct 2025 20:00:00 +0000

hifiberry-mpd (0.24.5.3) stable; urgency=low

  * Enhanced ACL permissions management for audio group access
  * Improved directory ownership handling for /var/lib/mpd
  * Refined mpd-set-permissions script with better error handling

 -- HiFiBerry <support@hifiberry.com>  Mon, 30 Sep 2025 14:00:00 +0000

hifiberry-mpd (0.24.5.2) stable; urgency=low

  * Add mpd-set-permissions script for ACL management
  * Fix MPD directory permissions and group ownership
  * Add manual pages for custom scripts
  * Fix systemctl path in maintainer scripts

 -- HiFiBerry <support@hifiberry.com>  Tue, 23 Sep 2025 08:16:07 +0100

hifiberry-mpd (0.24.5.1) stable; urgency=low

  * Bump to 0.24.5
  * Use Debian 13 (trixie)
  * MPD now runs as user

 -- HiFiBerry <support@hifiberry.com>  Tue, 22 Jul 2025 12:00:00 +0000

hifiberry-mpd (0.24.4.4) stable; urgency=low

  * Link SMB shares to mpd music directory

 -- HiFiBerry <support@hifiberry.com>  Mon, 22 Jul 2025 12:00:00 +0000

hifiberry-mpd (0.24.4.3) stable; urgency=low

  * Added debhelper systemd service integration for automatic service management
  * Improved package upgrade experience with proper service handling
  * Enhanced maintainer scripts following Debian best practices

 -- HiFiBerry <support@hifiberry.com>  Tue, 02 Jul 2025 15:00:00 +0000

hifiberry-mpd (0.24.4.2) stable; urgency=low

  * Added configure-mpd script for automatic mixer configuration
  * Updated systemd service to use runtime configuration
  * Service now automatically configures hardware/software mixer

 -- HiFiBerry <support@hifiberry.com>  Tue, 01 Jul 2025 12:00:00 +0000

hifiberry-mpd (0.24.4.1) stable; urgency=low

  * Build on stable

 -- HiFiBerry <support@hifiberry.com>  Sun, 30 Jun 2025 12:00:00 +0000

hifiberry-mpd (0.24.4) unstable; urgency=low

  * Initial Debian package release
  * Music Player Daemon optimized for HiFiBerry devices
  * Built with ALSA backend and Avahi/mDNS support
  * Includes both MPD daemon and MPC client
  * Custom configuration for HiFiBerry audio devices

 -- HiFiBerry <support@hifiberry.com>  Thu, 26 Jun 2025 12:00:00 +0000
