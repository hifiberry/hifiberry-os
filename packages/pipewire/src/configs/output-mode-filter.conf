# Output Mode & Balance Filter for WirePlumber 0.5+
# Provides mono/stereo/left/right output modes and balance control

wireplumber.components = [
  {
    name = output-mode-filter.lua, type = script/lua
    provides = custom.output-mode-filter
  }
]

wireplumber.profiles = {
  main = {
    custom.output-mode-filter = required
  }
}
