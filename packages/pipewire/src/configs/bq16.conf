# 16-Band Biquad EQ Filter for WirePlumber 0.5+
# Provides parametric EQ with 16 biquad filters

wireplumber.components = [
  {
    name = bq16.lua, type = script/lua
    provides = custom.bq16-eq
  }
]

wireplumber.profiles = {
  main = {
    custom.bq16-eq = required
  }
}
