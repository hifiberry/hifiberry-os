# HiFiBerry PipeWire configuration override for headless systems
# This file disables modules that require D-Bus session or X11
# Generated automatically by hifiberry-pipewire package

context.properties = {
    # Disable portal module (requires session bus)
    module.portal = false
    
    # Disable JACK D-Bus detection (requires session bus)
    module.jackdbus-detect = false
    
    # Disable X11 bell (requires X11 display)
    module.x11.bell = false
}
