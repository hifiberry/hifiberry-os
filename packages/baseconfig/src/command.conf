# Commands to execute during base configuration
# Format: command_to_execute
# Commands are run in order listed, with root privileges

# Configure HiFiBerry Web UI nginx integration
configure-webui setup
